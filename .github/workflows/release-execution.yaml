# =============================================================================
# 🚀 Release Execution Workflow
#
# Description:
#   A reusable workflow that processes prepared release data and executes
#   the release process including changelog generation and branch cleanup.
#
# Key Features:
#   • Metadata processing from .release-meta.json
#   • Release creation via conventional commits
#   • Branch merging and cleanup
#   • Release documentation generation
#
# Required Files:
#   • .release-meta.json with release configuration
#
# Dependencies:
#   • release-conventional.yaml
#
# Inputs:
#   • runner: CI runner selection
#
# Outputs:
#   • GitHub Release
#   • Updated branch state
#   • Release documentation
#
# Usage:
#   Called by release-run.yaml
# =============================================================================


name: 🚀 Release Execution Workflow

on:
  workflow_call:
    inputs:
      target:
        description: 'The target branch name for merge'
        required: false
        default: ${{ github.event.repository.default_branch }}
        type: string
      temporary:
        description: 'The temporary branch name for merge'
        required: false
        default: 'release'
        type: string
      runner:
        description: 'GitHub runner to execute the workflow (e.g., ubuntu-latest, self-hosted)'
        required: false
        default: ${{ vars.DEFAULT_RUNNER }}
        type: string
    outputs:
      previous_tag:
        description: "Previous tag from metadata"
        value: ${{ jobs.metadata.outputs.previous_tag }}
      new_tag:
        description: "New tag from metadata"
        value: ${{ jobs.metadata.outputs.new_tag }}
      prerelease:
        description: "Prerelease flag from metadata"
        value: ${{ jobs.metadata.outputs.prerelease }}
      merge_option:
        description: "Merge option from metadata"
        value: ${{ jobs.metadata.outputs.merge_option }}
      source_branch:
        description: "Source branch from metadata"
        value: ${{ jobs.metadata.outputs.source_branch }}

jobs:
  metadata:
    name: Read Metadata & Trigger Reusable Release Workflow
    uses: stetind/ppc-reusable-workflows/.github/workflows/release-checkout.yaml@__VERSION__
    with:
      runner: ${{ inputs.runner }}

  release:
    name: Create Release
    needs: metadata
    uses: stetind/ppc-reusable-workflows/.github/workflows/release-conventional.yaml@__VERSION__
    secrets: inherit
    with:
      tag: ${{ needs.metadata.outputs.new_tag }}
      prerelease: ${{ needs.metadata.outputs.prerelease || '' }}
      release_note_file: '.release_notes.md'
      runner: ${{ inputs.runner }}

  pull_request:
    needs: [ release, metadata ]
    name: 🧹 Create PR from `${{ needs.metadata.outputs.source_branch }}` to `${{ inputs.target }} `
    if: ${{ needs.metadata.outputs.source_branch !=  inputs.target &&  needs.metadata.outputs.merge_option != 'none' }}
    uses: stetind/ppc-reusable-workflows/.github/workflows/release-cleanup.yaml@__VERSION__
    secrets: inherit
    with:
      runner: ${{ inputs.runner }}
      source: ${{ needs.metadata.outputs.source_branch }}
      target: ${{ inputs.target }}
      title: Release ${{ needs.metadata.outputs.new_tag }}
      merge_option: ${{ needs.metadata.outputs.merge_option }}

  clean_release:
    name: 🧹 Clean Temporary branch
    needs: [ release, metadata ]
    runs-on: ${{ inputs.runner }}
    container: 'node:22.14'
    steps:
      - name: 🗑️ Delete `${{ inputs.temporary }}` Branch
        uses: stetind/ppc-github-composite-actions/.github/actions/delete-bracnh@v1.0.6
        with:
          token: ${{ secrets.WORKFLOW_TOKEN }}
          branch: ${{ inputs.temporary }}
