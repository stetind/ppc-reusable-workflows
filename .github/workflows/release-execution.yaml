# =============================================================================
# 🚀 Release Execution Workflow
#
# Description:
#   A reusable workflow that processes prepared release data and executes
#   the release process including changelog generation and branch cleanup.
#
# Key Features:
#   • Metadata processing from .release-meta.json
#   • Release creation via conventional commits
#   • Branch merging and cleanup
#   • Release documentation generation
#
# Required Files:
#   • .release-meta.json with release configuration
#
# Dependencies:
#   • release-conventional.yaml
#
# Inputs:
#   • runner: CI runner selection
#   • job_container: Container configuration
#
# Outputs:
#   • GitHub Release
#   • Updated branch state
#   • Release documentation
#
# Usage:
#   Called by release-run.yaml
# =============================================================================


name: 🚀 Release Execution Workflow

on:
  workflow_call:
    inputs:
      target:
        description: 'The target branch name for merge'
        required: false
        default: 'main'
        type: string
      runner:
        description: 'GitHub runner to execute the workflow (e.g., ubuntu-latest, self-hosted)'
        required: false
        default: ${{ vars.DEFAULT_RUNNER }}
        type: string
      job_container:
        description: 'Docker container image to run the job (e.g., node:22.14)'
        required: false
        default: ${{ vars.DEFAULT_NODE_CONTAINER }}
        type: string
    outputs:
      previous_tag: ${{ jobs.metadata.outputs.previous_tag }}
      new_tag: ${{ jobs.metadata.outputs.new_tag }}
      prerelease: ${{ jobs.metadata.outputs.prerelease }}
      merge_option: ${{ jobs.metadata.outputs.merge_option }}
      source_branch: ${{ jobs.metadata.outputs.source_branch }}

jobs:
  metadata:
    name: Read Metadata & Trigger Reusable Release Workflow
    uses: stetind/ppc-reusable-workflows/.github/workflows/release-checkout.yaml@v9.0.0
    secrets:
      GH_TOKEN: ${{ secrets.WORFLOW_TOKEN }}
    with:
      runner: ${{ inputs.runner }}
      job_container: ${{ inputs.job_container }}

  release:
    name: 📟 Final Release
    needs: metadata
    uses: stetind/ppc-reusable-workflows/.github/workflows/release-conventional.yaml@v9.0.0
    secrets: inherit
    with:
      tag: ${{ needs.metadata.outputs.previous_tag }}
      prerelease: ${{ needs.metadata.outputs.prerelease }}
      runner: ${{ inputs.runner }}
      job_container: ${{ inputs.job_container }}

  clean:
    name: 🧹 Merge and clean branches
    needs: [release, metadata]
    uses: stetind/ppc-reusable-workflows/.github/workflows/release-clean.yaml@v9.0.0
    secrets:
      GH_TOKEN: ${{ secrets.WORKFLOW_TOKEN }}
    with:
      title: Release ${{ needs.metadata.outputs.new_tag }}
      merge_option: ${{ needs.metadata.outputs.merge_option }}
      source: ${{ needs.metadata.outputs.source_branch }}
      target: ${{ inputs.target }}
      runner: ${{ inputs.runner }}
      job_container: ${{ inputs.job_container }}