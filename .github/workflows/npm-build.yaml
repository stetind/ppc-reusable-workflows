name: Npm - Build

on:
  workflow_call:
    inputs:
      node_environment:
        description: Set the node environemtn (dev|production)
        type: string
        default: production
        required: true
      artifact_name:
        description: The artifact name, that contains the code which is being to be published
        type: string
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    env:
      NODE_ENV: ${{ inputs.node_environment }}

    steps:
      - name: 🛫 Checkout
        uses: actions/checkout@v4

      - name: Setup Node and NPM
        uses: stetind/web-setup-node-npm-action@v1

      - name: 🔧 Install Global Deployment Dependencies
        uses: stetind/ppc-npm-global-dependencies-action@v1.1

      - name: 🏍 Building Environment
        run: npm run build --if-present && npx browserslist@latest --update-db

      - name: ✅ Artifact creation
        if: ${{ success() }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact_name }}
          retention-days: 1
          path: ${{github.workspace}}/*
