name: 🚀 Finalize Auto Release
run-name: 🚀 Finalize Auto Release

env:
  TEMP_BRANCH: releases

on:
  push:
    branches:
      - ${{ env.TEMP_BRANCH }}

jobs:
  create_release:
    name: Final Release
    uses: stetind/ppc-reusable-workflows/.github/workflows/auto-release-based-conventional-commits.yaml@releases
    secrets: inherit
    with:
      branch: ${{ env.TEMP_BRANCH }}
